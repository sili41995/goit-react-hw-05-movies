import statuses from './statuses';

const initialState = {
  movies: null,
  movie: null,
  cast: null,
  status: statuses.idle,
  reviews: [],
  // searchQuery: '',
  // page: 1,
  // images: [],
  // totalImages: null,
};

export default initialState;
